---
import type { ProjectItem } from '../../data/content';
import ProjectCard from '../ui/ProjectCard.astro';
import SectionTitle from '../ui/SectionTitle.astro';

interface Props {
  title: string;
  projects: ProjectItem[];
}

const { title, projects } = Astro.props;
---

<section id="projects">
  <div class="page-container section-shell section-stack">
    <SectionTitle text={title} />
    <div class="mx-auto grid w-full max-w-[1080px] grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-3 max-md:max-w-[620px] max-md:box-border max-md:gap-4 max-md:px-1 max-md:py-2">
      {projects.map((project) => (
        <ProjectCard project={project} />
      ))}
    </div>
  </div>
</section>
